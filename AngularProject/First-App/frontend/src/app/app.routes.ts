import { Routes } from '@angular/router';
import { EventOrganizerDashboard } from './event-organizer-dashboard/event-organizer-dashboard';

export const routes: Routes = [
  { path: '', component: EventOrganizerDashboard }
];